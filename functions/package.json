{
  "name": "grama-khata-functions",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "eslint .",
    "serve": "npx firebase-tools emulators:start --only functions,firestore",
    "deploy": "npx firebase-tools deploy --project grama-khata-39924 --only functions,firestore:rules,firestore:indexes"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
